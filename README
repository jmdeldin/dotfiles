= Dotfiles

These are my personal configuration files. Feel free to pilfer.

= Installation

Use GNU Stow (or manually symlink):

  $ git clone git://github.com/jmdeldin/dotfiles.git ~/etc
  $ stow -S etc -t $HOME -v --ignore='README'

= Uninstallation

  $ stow -D etc -t $HOME -v
